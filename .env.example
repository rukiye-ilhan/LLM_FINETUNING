# Optional Hugging Face token for faster downloads / higher rate limits
HF_TOKEN=

# Optional runtime environment label
APP_ENV=local

# Optional future OpenAI-compatible LLM endpoint configs
OPENAI_API_KEY=
OPENAI_BASE_URL=
OPENAI_MODEL=